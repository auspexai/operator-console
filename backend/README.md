# auspexai-operator-console (backend)

FastAPI + uvicorn backend serving the SvelteKit-built operator console UI alongside its JSON API.

See the [top-level README](../README.md) for the project overview and the design doc (Documentation/AuspexAI/v0.1.0/operator_console_design.md, currently local-only) §16 for the build plan.

Apache-2.0; consistent with the §5.18 separate-codebases ratification.
